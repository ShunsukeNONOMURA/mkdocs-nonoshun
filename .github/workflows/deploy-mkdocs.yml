name: deploy-mkdocs

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # ソースコードを取得
      - name: checkout
        uses: actions/checkout@v4

      # Python 環境をセットアップ
      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # MkDocs 関連パッケージをインストール
      - name: install mkdocs deps
        run: |
          python -m pip install --upgrade pip
          pip install \
            mkdocs \
            mkdocs-material \
            plantuml-markdown \
            python-markdown-math \
            pygments \
            pymdown-extensions \
            mkdocs-awesome-pages-plugin \
            mkdocs-autolinks-plugin \
            mkdocs-macros-plugin

      # MkDocs を build（成果物は volume/mkdocs）
      - name: build mkdocs for gh-pages
        run: |
          cd volume
          mkdocs build --clean --site-dir mkdocs

      # gh-pages に docs/mkdocs として deploy
      - name: deploy to gh-pages under docs/mkdocs
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: volume/mkdocs
          destination_dir: docs/mkdocs
          force_orphan: true